// 1. In a new repo, create two files: `lib.js` (or named something else) and `app.js`.
// 2. lib.js should export an object with related functions.
// 3. Import lib.js in app.js and call each of the related functions. Use `console.log`
//  to confirm the functions are returning values when called.

var foo = require("./var.js");

console.log(foo.prime(-2));

// var result = people.removeBadPeople([{name: 'A'}]);
